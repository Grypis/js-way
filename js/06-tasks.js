/* const sub = 'free';
if (sub === 'pro' || sub === 'vip') {
  console.log('Access successful');
} else {
  console.log('Access unsuccessful');
}

const subScr =
  sub === 'pro' || sub === 'vip'
    ? console.log('Access successful')
    : console.log('Access unsuccessful');
 */

/* const isOnline = true;
const isFriend = true;
const isDnd = false;
const canOpenChat = isOnline && isFriend && !isDnd;
console.log('Можно відкрити чат? ', canOpenChat); */

/* const priceTicket = 150;

const userAge = 10;
const userBalance = 100;

const isAdmin = false;
const validAge = userAge >= 18; // false
const validBalance = userBalance > priceTicket;

if (validAge && validBalance && isAdmin) {
  console.log('God');
} else {
  console.log('Get out!');
}
 */
