const numbers = [1, 5, 2, 4, 3];
/* const greaterThenTwo = numbers.filter(el => el > 2);
console.log(greaterThenTwo);
const multByThree = greaterThenTwo.map(el => el * 3);
console.log(multByThree);
const sorted = multByThree.toSorted((a, b) => a - b);
console.log(sorted); */

/* const toChaining = numbers
  .filter(el => el > 2)
  .map(el => el * 3)
  .toSorted((a, b) => a - b);
console.log(toChaining); */

/* const result = numbers
  .filter(el => el % 2 === 0)
  .map((el, i, arr) => {
    return el * arr.length;
  });
console.log(result); */

/* const getModelOnSale = cars => {
  return cars.filter(car => car.onSale).map(car => car.model);
};
console.log(getModelOnSale(cars)); */
