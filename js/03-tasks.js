/* const hours = 14;
const minutes = 26;
/* const time1 = minutes === 0 ? `${hours} г.` : `${hours} г. ${minutes} хв.`;
console.log(time1); */
/* const time2 = `${hours} г.` + minutes === 0 ? '' : `${minutes} хв.`;
console.log(time2); */

/* let timestring;
if (minutes === 0) {
  timestring = `${hours} г.`;
} else {
  timestring = `${hours} г. ${minutes} хв.`;
}
console.log(timestring);
 */

/* const daysUntilDeadline = 6;
if (daysUntilDeadline === 0) {
  console.log('Today');
} else if (daysUntilDeadline === 1) {
  console.log('Tomorrow');
} else if (daysUntilDeadline === 2) {
  console.log('The day after tomorrow');
} else {
  console.log('Date in the future');
}
 */
